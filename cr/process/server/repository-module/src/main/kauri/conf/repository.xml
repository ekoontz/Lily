<?xml version="1.0"?>
<repository>
  <interface>default</interface>
  <nameserver>default</nameserver>

  <!-- Port on which to bind the avro server, 0 will bind to an ephemeral port. -->
  <port>0</port>

  <!-- Filesystem to use to store blobs -->
  <blobFileSystem>hdfs://localhost:9000/lily/blobs</blobFileSystem>
</repository>